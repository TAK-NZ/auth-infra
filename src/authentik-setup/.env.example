# Authentik API configuration
AUTHENTIK_API_ENDPOINT=https://auth.example.com
AUTHENTIK_API_TOKEN=your_api_token_here

# TAK server configuration
TAK_SERVER_DOMAIN=ops.example.com
ENROLL_SERVER_DOMAIN=enroll.example.com

# Provider and application configuration
PROVIDER_NAME=TAK Enrollment OAuth Provider
APP_NAME=Enroll a device
APP_SLUG=tak-enrollment

# Flow configuration (leave empty to disable)
AUTHN_FLOW=default-authentication-flow
AUTHZ_FLOW=default-provider-authorization-implicit-consent
INVALIDATION_FLOW=default-provider-invalidation-flow

# Optional: Path to icon file (relative or absolute)
ICON_PATH=./TAK-Enroll.png

# Enable debug mode
DEBUG=false