services:
  auth-infra-server:
    platform: linux/amd64
    image: ghcr.io/tak-nz/auth-infra-server
    build:
      context: .
      dockerfile: ./docker/amd64/Dockerfile.authentik-server
  auth-infra-ldap:
    platform: linux/amd64
    image: ghcr.io/tak-nz/auth-infra-ldap
    build:
      context: .
      dockerfile: ./docker/amd64/Dockerfile.authentik-ldap
